
Estamos en lo siguiente
    Ya tenemos Validación Schema, Forms, React, Bootsrap, Sqlite, Eelectron.


Lo que tenemos que hacer es lo siguiente, cuando la app cargue y la ventana aparece, el database context
engloba todos los componentes de react, queda loading.

Ese en principio tiene que leer las listas de categorias, tipos, etc de la DB y quedarselas. Si eso esta ok,
que muestre un form de insert de transaccion.

Para esto ya expusimos del "back end" las operaciones crud basicas. Hace falta agregar la carga de las listas.

Tener la app conectada a la db y con un form de carga de transacciones bien validado los campos seria la V1.